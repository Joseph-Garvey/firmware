# Per-deployment node-ID -> name map, auto-loaded by mqtt_slm_bridge.py.
# Not secret, but deployment-specific -- keep it local; slm-labels.example.json
# is the committed template.
slm-labels.json

# Dedup cache persisted by mqtt_slm_bridge.py across restarts (runtime state).
.slm-dedup-state.json
.slm-dedup-state.json.tmp
